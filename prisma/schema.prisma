generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model clothes {
  id          Int      @id @default(autoincrement())
  description String   @db.Text
  category    String   @db.Varchar(50)  // Fixed: Varchar not VarChar
  price       Decimal  @db.Decimal(10, 2)
  image       String   @db.Text
  created_at  DateTime @default(now())
  quantity    Int      @default(1)
}

model order_items {
  order_item_id Int      @id @default(autoincrement())
  order_id      Int
  product_id    Int
  quantity      Int
  price         Decimal  @db.Decimal(10, 2)
  product_image String   @db.Varchar(255)  // Fixed
  status        String   @db.Varchar(255)  // Fixed
  delivery      String   @db.Varchar(200)  // Fixed
  date_created  DateTime @default(now())

  @@index([order_id], name: "order_id")
  @@index([product_id], name: "product_id")
}

model orders {
  order_id   Int      @id @default(autoincrement())
  user_id    String   @db.Varchar(200)  // Fixed
  name       String   @db.Varchar(255)  // Fixed
  email      String   @db.Varchar(255)  // Fixed
  address    String   @db.Varchar(255)  // Fixed
  phone      String   @db.Varchar(15)   // Fixed
  created_at DateTime @default(now())
}

model cart {
  id          Int     @id @default(autoincrement())
  description String  @db.Varchar(100)  // Fixed
  price       Decimal @db.Decimal(10, 2)
  image       String  @db.Varchar(100)  // Fixed
  quantity    Int
  clothe_id   Int
  email       String  @db.Varchar(255)  // Fixed
  
  user user @relation(fields: [email], references: [email], onDelete: Cascade)

  @@index([email], name: "fk_cart_user_email")
}

model user {
  user_id      Int      @id @default(autoincrement())
  full_name    String
  phone_number Int
  username     String
  email        String   @unique @db.Varchar(255)  // Fixed
  location     String
  password     String
  role         String?  @default("user") @db.Varchar(50)  // Fixed
  createdAt    DateTime @default(now())
  cart         cart[]
}

model sessions {
  id        String   @id
  sid       String   @unique
  data      String   @db.Text
  expiresAt DateTime

  @@map("sessions")
  @@index([expiresAt], name: "idx_sessions_expires_at")
}